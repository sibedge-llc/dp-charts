apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eventer.fullname" . }}-test-connection"
  labels:
    {{- include "eventer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eventer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
